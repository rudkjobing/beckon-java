# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                           controllers.Angular.index()

# User
POST        /user                       controllers.Application.addPerson()

GET         /people                     controllers.Application.people()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file               controllers.Assets.at(path="/public", file)

#WEB
#POST        /api/signUp                 controllers.Angular.signUp()
#POST        /api/signIn                 controllers.Angular.signIn()
#POST        /api/getSecurityContext     controllers.Angular.getSecurityContext